# PostgreSQL connection string (use Neon/Supabase/Railway in production)
# Local example: postgresql://postgres:password@localhost:5432/riyaz_portfolio
# Neon/Vercel example: postgresql://user:password@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require
DATABASE_URL="postgresql://user:password@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require"

# Used once by the seed script to create your admin login. Change the password after first login.
ADMIN_EMAIL="moriyaz933@gmail.com"
ADMIN_PASSWORD="changeme123"

# Secret used to sign admin session JWTs — generate a random 32+ char string
# You can generate one with: openssl rand -base64 32
JWT_SECRET="replace-this-with-a-long-random-string"
